import { SizingHandleDirective } from './sizing-handle.directive';

describe('SizingHandleDirective', () => {
  it('should create an instance', () => {
    const directive = new SizingHandleDirective();
    expect(directive).toBeTruthy();
  });
});
